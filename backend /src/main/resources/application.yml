spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/torrosplit
    username: postgres
    password: postgres123
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
